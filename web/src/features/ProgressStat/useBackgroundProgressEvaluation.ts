export const useBackgroundProgressEvaluation = () => {
  // 1. read all conversations once
  // 2. read all daily answers once
  // 3. Create queue of evaluations to do
  // Step by step evaluate and update progress stats in DB
};
