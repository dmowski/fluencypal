import { Stack, Typography } from '@mui/material';

export const AliasPage = () => {
  return (
    <Stack>
      <Typography variant="h4" gutterBottom>
        Alias Game
      </Typography>
    </Stack>
  );
};
