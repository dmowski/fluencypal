'use client';

import { Stack, Typography } from '@mui/material';

export const PlayersSetup = () => {
  return (
    <Stack spacing={3} alignItems="center" sx={{ py: 4 }}>
      <Typography variant="h4">Players Setup</Typography>
      <Typography variant="body1" color="text.secondary">
        Coming soon...
      </Typography>
    </Stack>
  );
};
