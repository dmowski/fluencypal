export interface CTASectionProps {
  title: string;
  subTitle: string;
  buttonTitle: string;
  buttonHref: string;
}

/** Interview Landing CTA Section */
export const CTASection = ({}: CTASectionProps) => {
  return <></>;
};
