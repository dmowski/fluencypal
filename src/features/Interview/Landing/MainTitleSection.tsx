export interface MainTitleSectionProps {
  label: string;
  title: string;
  subtitle: string;
  buttonHref: string;
  buttonTitle: string;
}

/** Interview Landing Main Title Section */
export const MainTitleSection = ({}: MainTitleSectionProps) => {
  return <></>;
};
