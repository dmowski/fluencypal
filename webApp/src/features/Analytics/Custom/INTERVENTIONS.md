# Analytics interventions

Log every product change made because of custom analytics. This stops repeating the same experiment.

| Date       | Hypothesis                                                                                               | Change                                                                                                                                          | Metric to watch                                                                       | Status  | Result                                                                                                                                                                                          |
| ---------- | -------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-28 | Baseline instrumentation                                                                                 | Scroll, time-on-landing, CTA intent, conversation_start, paywall, bot filter, GEO/UTM                                                           | Funnel + landing duration + CTA clicks                                                | shipped | —                                                                                                                                                                                               |
| 2026-08-29 | Crawler page_views look like Chrome and poison practice→speak                                            | Do not persist first `page_view` until click/scroll/leave/identify; hide lone page_view visitors from reports                                   | Share of practice visitors with eventCount=1                                          | shipped | —                                                                                                                                                                                               |
| 2026-08-31 | Landing→app visits split; CTA counts were every in-app click; duration/scroll/funnel mixed lifetime data | First-party visitor id + `fpv` handoff; CTA from element only; visible-time leaves; UTC day-scoped export                                       | Share of landing visitors who also `reachedApp` on the same id; landing CTA counts    | shipped | —                                                                                                                                                                                               |
| 2026-08-31 | Quiz recordAbout and lessons used voice but looked like “never spoke”; SEO queries unknown               | `speech_start` (quiz/lesson/conversation) + Search Console export on `analytics:export`                                                         | Quiz visitors with `speech_start` vs last path recordAbout; GSC `available`           | shipped | —                                                                                                                                                                                               |
| 2026-09-01 | Scenario Play CTA dies on the 3-step auth wall                                                           | Skip features/agreement when `rolePlayId` is set; first screen is Google with “Sign in to play Alias” / “Sign in to start {scenario}”           | Share of `practice?rolePlayId=*` visitors who `identify`                              | shipped | 2026-09-02: 2/8 new roleplay visitors identified; both spoke. Remaining drop is 0–10s on Google, not extra screens.                                                                             |
| 2026-09-02 | Google sign-in before any audio still kills scenario traffic; they need to hear the scene first          | Autoplay AI opening line (audio + transcript) on `practice?rolePlayId=*` Google screen; sign-in still required to reply                         | Identify rate among new `practice?rolePlayId=*` visitors                              | keep    | 2026-09-03: 3/6 new roleplay visitors identified (was 2/8); all 3 spoke. Non-identifiers stayed 17–55s and tapped Hear, then left.                                                              |
| 2026-09-03 | Hear-clickers still refuse “Sign in”; Privacy sits between the clip and Google                           | Roleplay primary CTA is “Continue to talk” (still Google); legal links move below the buttons                                                   | Identify rate among new `practice?rolePlayId=*` visitors                              | shipped | —                                                                                                                                                                                               |
| 2026-09-04 | Quiz visitors bounce at teacherSelection because Google appears before they pick or hear a voice         | Remove AuthWall from quiz teacherSelection; keep chosen voice locally until sign-in; AuthWall stays on recordAbout                              | Share of new `/quiz` visitors who reach `recordAbout` or `speech_start` (quiz)        | shipped | —                                                                                                                                                                                               |
| 2026-09-05 | Interview-phrases readers finish the article and never start speaking                                    | End-of-article CTA on interview-phrases posts → `practice?rolePlayId=job-interview` (`blog-interview-cta`)                                      | Share of `/blog/phrases-for-an-interview-in-english` visitors who `reachedApp`        | shipped | —                                                                                                                                                                                               |
| 2026-09-06 | Quiz visitors sign in and sit on recordAbout without pressing the mic                                    | Autoplay chosen teacher asking “Tell me about yourself” (audio + Hear) on quiz `recordAbout`; stop clip when they record                        | Share of new `/quiz` visitors who reach `recordAbout` and then `speech_start` (quiz)  | shipped | —                                                                                                                                                                                               |
| 2026-09-07 | Scenario SEO readers scroll the article and never start the scene; footer sent them to quiz              | End-of-article + footer Play CTA on scenario pages → `practice?rolePlayId={id}` (`scenario-play-cta` / `scenario-footer-cta`)                   | Share of new `/scenarios/*` visitors who `reachedApp`                                 | shipped | —                                                                                                                                                                                               |
| 2026-09-09 | Quiz visitors agree + hit Google/email on `before_recordAbout` and never reach `recordAbout` autoplay    | Google-first auth with teacher clip + Continue to talk; auto-advance to `recordAbout` after identify                                            | Share of new `/quiz` visitors who leave `before_recordAbout` vs `speech_start` (quiz) | shipped | —                                                                                                                                                                                               |
| 2026-09-09 | Daily report still needed timeline sampling for quiz-step drop, Hear-then-leave, and scenario vs quiz    | Export `quizSteps` / `entry` / `identifyPaths` / `appCtaIds`; `data-analytics` on Google, email, Hear                                           | Named `appCtaIds` + quizSteps in `analytics:export`                                   | shipped | —                                                                                                                                                                                               |
| 2026-09-09 | Paid search for speaking practice will bounce on a generic “AI language learning” hero                   | Homepage: “You already know English / Now start speaking it”; CTA “Stop Studying. Start Speaking.”; SEO title matches speaking practice         | Share of new `/` visitors who click `hero-cta` / `reachedApp`                         | shipped | 2026-09-12: 5 `hero-cta` / 6 `reachedApp` among 87 home entries — almost all YouTube Android WebView to `/` or `/scenarios` (104 YT, 3 app, 0 speech). Non-YT home 23 → 3 hero / 4 app / 1 speech. Do not rewrite hero again for this YT mix. |
| 2026-09-10 | Alias hear-clickers still refuse Google; they will talk if they can reply before sign-in                 | Record one guest reply on `practice?rolePlayId=*` (`reply-first-line`); Google still required to continue; inject reply after Start (not Alias) | Share of new `practice?rolePlayId=*` visitors with `speech_start` (conversation)      | keep    | 2026-09-11: 0/3 speech. 2026-09-12: 2 new roleplay `speech_start`. 2026-09-13: 4/4 new Reply → conversation speech, 0 identify, 0 `conversation_start`. Play-without-Reply still 0/3 speech (2–3s leave). |
| 2026-09-11 | Quiz speakers finish `goalReview` and die on empty `/practice`                                           | After quiz plan confirm, go to `/practice?justTalk=open` and auto-start Just Talk with the chosen teacher                                       | Share of new `/quiz` visitors who reach `goalReview` and then `conversation_start`    | keep    | 2026-09-12: 2/2 `goalReview` hit `justTalk=open`. 1/2 `conversation_start` (ES, long session). AR opened Just Talk twice, Enable mic + End call, no conversation — leftover is mic, not empty practice. |
| 2026-09-12 | Quiz visitors agree + hit Google on `before_recordAbout` and never reach `recordAbout` autoplay          | Guest first `recordAbout` clip (`record-about-guest`); Google still required to continue; transcribe + write `aboutUserTranscription` after identify | Share of new `/quiz` visitors who reach `before_recordAbout` and then `speech_start` (quiz) | keep    | 2026-09-13: 6/7 `before_recordAbout` → quiz `speech_start` (9/18 new quiz hit `record-about-guest`). Then 1/7 identify (email), 0 `goalReview` / conversation. Leftover is Google after they already spoke. |
| 2026-09-14 | Guest quiz speakers hit Google on the same screen and leave                                              | After `record-about-guest`, teacher reaction + `quiz-guest-continue`; skip follow-ups; AuthWall only on `goalReview` / Just Talk                    | Share of new `/quiz` visitors who reach `before_recordAbout` and then `goalReview`    | shipped | 2026-09-14 (same-day, not keep yet): 11/17 new `before_recordAbout` → `goalReview` (was 0/7 on 09-13). 14 `quiz-guest-continue`. Then 7/12 `goalReview` identify; 1 conversation. Leftover is Google at `goalReview`, then Just Talk without `conversation_start`. |
| 2026-09-14 | Quiz→Just Talk visitors grant nothing / End call and land on empty `/practice`                           | Persistent `JustTalkHandoffScreen` while `justTalk=open`; keep param until first user message; `enable-mic-just-talk` retry instead of dashboard   | Share of quiz→`justTalk=open` visitors with `conversation_start`                      | shipped | —                                                                                                                                                                                               |
| 2026-09-15 | Guest quiz speakers hit Google on `goalReview` and never reach Just Talk / conversation                  | Remove AuthWall on `goalReview`; anonymous Firebase for WebRTC; client guest limit after first user reply; Google only to continue                 | Share of new `/quiz` visitors who reach `goalReview` and then `conversation_start`    | keep    | 2026-09-15→17: 31/33 `goalReview` → `justTalk=open`; 11/33 `conversation_start`; only 4/33 auth. Auth at plan confirm is fixed. Leftover: 25/36 on Just Talk with `enable-mic-just-talk` taps but no (or aborted) conversation — second mic gate after quiz speech. |
| 2026-09-17 | Quiz speakers hit a second Enable-mic gate on Just Talk after they already spoke in the quiz             | On `goalReview` Start Speaking, prime mic in the same click + auto-start Just Talk only when `fp_justTalkAutoStart` is set; cold `/practice?justTalk=open` keeps handoff (no permission dialog until Enable mic) | Share of `goalReview` → `justTalk=open` visitors with `conversation_start`            | shipped | —                                                                                                                                                                                               |
| 2026-09-17 | Guests vanish on Exit; signed-in free users get a day trial then soft limits                             | Remove trial grant; guest Exit → sign-in wall; free tier paywall after 10 user messages; landing copy says no trial                                 | Guest Exit → `conversation-guest-auth`; free users `paywall_view` at 10 user msgs     | shipped | —                                                                                                                                                                                               |
| 2026-09-17 | Funnel cannot show why people stall (unlabeled clicks, no mic/call/auth outcomes, no page context)       | Named in-app CTAs + `uiContext` digest on view/click/leave + `permission` / `call_state` / `auth_attempt` / `ui_error` / `dead_click`; export rage/dead/screens | Just Talk leftover: `permission` denied vs `call_state` failed vs unlabeled mute; `rageClickVisitors` | shipped | —                                                                                                                                                                                               |
| 2026-09-17 | Quiz Just Talk ignored chosen teacher / learn language and started muted, so guests spent free turns on “I want a man / can you hear me” | Snapshot voice + learn language on Start Speaking; auto-start unmuted; explicit language wins over page locale | Auto-start uses quiz teacher + learn language; `call-mic-toggle` aria-pressed true after Start Speaking | shipped | — |
| 2026-09-18 | Quiz settings lived in localStorage until goalReview anonymous auth, so Just Talk could start as Shimmer / page language | Create anonymous user at quiz entry; teacher selection waits for uid and writes Firestore; remove pending voice/language localStorage | Teacher selection Continue requires `auth.uid` + saved `teacherVoice`; Just Talk uses `userSettings` | shipped | — |
| 2026-09-18 | Guest “about you” clip lived in sessionStorage until later identify, so it could vanish after anonymous auth already existed | Transcribe the guest clip immediately and write `aboutUserTranscription` on the survey; skip-follow-up uses survey text | `before_recordAbout` Continue after guest speech writes survey about text; no `fp:quizGuestAboutText` | shipped | — |
| 2026-09-18 | Signed-in users still got a longer interview (`recordAbout` + two follow-ups) while guests took one clip | Same one-clip quiz for everyone; drop follow-ups, `recordAbout`, and unused paywall/extra steps | All `/quiz` visitors use `before_recordAbout` → `before_goalReview` → `goalReview` | shipped | — |
| 2026-09-18 | People tap Reply on `before_recordAbout` with no `permission` event (WebView / first-ask after a failed Reply is a hard deny) | Dedicated `micPermission` quiz step after teacher selection: why-mic copy + `mic-permission-grant` before the clip | Share of `teacherSelection` → `micPermission` with `permission:granted` then `before_recordAbout` / quiz `speech_start` | shipped | — |
| 2026-09-18 | Guest 10-message cap opened Stripe checkout without Google/email because paywall treated anonymous Firebase as authorized | Payment modal shows AuthWall until identified; Stripe checkout rejects anonymous tokens and attaches the session to the token uid | Guest `paywall_view` without `identify`; checkout_start only after Google/email | shipped | — |
| 2026-09-18 | Teacher Continue stayed disabled because anonymous sign-in waited for React `auth.loading` then a second Identity Toolkit round trip | Start `ensureAnonymousAuth` at Firebase init (still after `authStateReady`); mint ID token immediately; keep Continue gated on real uid + saved `teacherVoice` | Share of `teacherSelection` visitors who click Continue / reach `micPermission` | shipped | — |
| 2026-09-19 | Quiz speakers reach Just Talk, hear a new-topic lecture, and freeze (OK / Huh / L1 / silence) | First quiz call is mode `quiz-talk` with a prompt that reacts to `aboutUserTranscription`; dashboard Just Talk stays `talk` | Share of `goalReview` → `justTalk=open` visitors with `conversation_start`; first user turn more than one word | shipped | — |
| 2026-09-20 | Quiz clips are already a sentence (median 7 words) but the first live follow-up is still an open “tell me more,” so they freeze at 1–2 words | When the about-you clip has 5+ words, `quiz-talk` first turn is a yes/no or A/B about that clip; short/empty clips do not invent a choice | First live user turn more than one token; `goalReview` → `conversation_start` | shipped | — |
| 2026-09-20 | Quiz about-you clips are one generic line (“I want to be better”), so the plan and first call have nothing to work with | Expand `before_recordAbout` subtitle (and spoken prompt) to ask for 2–3 sentences: who they talk to, what feels hard | Quiz `aboutUserTranscription` word count / specificity; fewer one-line clips | shipped | — |
| 2026-09-21 | Quiz-talk visitors hear the first A/B and freeze (17/69 no user turn) even though What to say? exists | Auto-show a tappable `quiz-talk-suggested-reply` chip after the first teacher line; dashboard `talk` stays tap-to-ask | Share of `goalReview` → `justTalk=open` visitors with `conversation_start`; `quiz-talk-suggested-reply` taps | measured | 2026-09-21→22: 50/52 new `goalReview` → `justTalk`; 39/52 `conversation_start` (was ~11/33 on 09-15→17). Chip taps 2/52 (plus 8 `call-what-to-say`). First quiz-talk turn still 26/59 one–two words, 8/59 no user turn. Chip unused; leftover is iOS Start Speaking → `justTalk` with no `call_state`. |
| 2026-09-22 | iOS Start Speaking lands on `justTalk=open` with no `call_state` because `fp_justTalkAutoStart` in sessionStorage does not survive the navigation | Carry consume-once `autoStart=1` on the practice URL (still keep the session flag); cold `justTalk=open` without the param stays handoff | Share of `goalReview` → `justTalk=open` visitors with `call_state` connected / `conversation_start`, especially iOS | measured | 2026-09-22T17:55Z→09-23T17:48Z: non-iOS 46/49 `justTalk` connected, 39/49 `conversation_start`. iOS 0/7 connected, 0/7 conversation. They sit on `practice.justTalk` (median ~37s) with no `practice.justTalkHandoff` and no `enable-mic-just-talk`. URL flag did not start the iOS call. |
| 2026-09-23 | iOS auto-start never reaches `call_state` and the Loading cover hides Enable mic, so the mic request never runs inside a tap | Keep `JustTalkHandoffScreen` up until the call connects; Enable-mic tap supersedes the in-flight auto-start | Share of iOS `goalReview` → `justTalk=open` visitors with `call_state` connected | shipped | — |
| 2026-09-23 | Quiz-talk keeps asking the learner to repeat the same sentence (prompt says “starter sentence they can repeat” on the first turn and again if they freeze) | First turn is a yes/no or A/B only; after any reply ask a new question in different words; never “repeat” | First quiz-talk user turns move to a new question; fewer one-line recitations | shipped | — |
| 2026-09-23 | Choosing a native language reloads the quiz with `location.assign` and a delayed `scrollIntoView`; iOS dies with no Sentry event | Client `router.push` for the locale switch; drop the delayed scroll on the language list; `scrollIntoView` without `behavior: 'instant'` | Quiz visitors who leave `nativeLanguage` still reach `teacherSelection` on iOS | shipped | — |
| 2026-09-23 | After the free-message wall, iOS Google sign-in shows “Continue to payment” and returns to the same screen still anonymous. Production: 6 `google:opened`, 0 success, 0 error. iOS drops the result stored on `dark-lang.firebaseapp.com` | Button says Sign in with Google; `signInWithRedirect`; finish that result before another anonymous user; proxy `/__/auth` on `app.fluencypal.com` and set `authDomain` to that host | Guest who hits the 10-message wall can sign in with Google on iOS and reach the plans | shipped | — |
| 2026-09-24 | Teacher-selection visitors audition voices and never tap Continue because that button sits under the voice grid | Pin Continue with the same sticky footer other quiz steps use. Export `insights.teacherContinue`: enabled/disabled × in view/offscreen | Share of `quiz.teacherSelection` visitors with `enabledInView` who then reach `micPermission` | shipped | — |

Status: `proposed` → `shipped` → `measured` → `keep` / `reverted`.

Rules:

- Do not propose a change that is already `shipped` or `proposed` for the same hypothesis unless the measured result is in.
- After shipping, wait at least one full day of traffic before judging.
- One primary metric per change.
